#
# Be sure to run `pod lib lint NimiqStats.podspec' to ensure this is a
# valid spec and remove all comments before submitting the spec.
#
# Any lines starting with a # are optional, but encouraged
#
# To learn more about a Podspec see http://guides.cocoapods.org/syntax/podspec.html
#

Pod::Spec.new do |s|
    s.name             = "NimiqStats"
    s.version          = "1.0.0"

    s.summary          = "NimiqStats"
    s.description      = <<-DESC
                         No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
                         DESC

    s.platform     = :ios, '7.0'
    s.requires_arc = true

    s.framework    = 'SystemConfiguration'

    s.homepage     = "https://github.com/KunaniGaming/NimiqStats"
    s.license      = "Proprietary"
    s.source       = { :git => "https://github.com/KunaniGaming/NimiqStats.git", :tag => "#{s.version}" }
    s.author       = { "KunaniGaming" => "darin@kunani.com" }

    s.source_files = 'NimiqStats/**/*.{m,h}'
    s.public_header_files = 'NimiqStats/**/*.h'


    s.dependency 'AFNetworking', '~> 3'
    s.dependency 'JSONModel', '~> 1.2'
    s.dependency 'ISO8601', '~> 0.6'
end

